COMMAND_START|joblist
#Convert image to ~15-second video with native stereo audio using MiniMax H3 (about 3x the render time of the 5s preset).
#DEFAULT path uses the turbo distill LoRA workflow (8 steps, ~2x faster); use the
#"Image To Video (MiniMax H3 Quality) 15s" preset for the full 20-step render.
%vid_width%="864"
%vid_height%="480"
%vid_length%="362"

img_to_video_minimax_h3_turbo.json @upload|image1|input1| @replace|"width": 864|"width": %vid_width%| @replace|"height": 480|"height": %vid_height%| @replace|"length": 124|"length": %vid_length%|
COMMAND_END
