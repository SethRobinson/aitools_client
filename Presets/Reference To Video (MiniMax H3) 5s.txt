COMMAND_START|joblist
#Generate a ~5-second video with audio featuring the SUBJECT(s) of 1-9 input photos (MiniMax H3 Ref2VA).
#TURBO default: lightx2v's Ref2V 4-step distill LoRA at 8 steps (euler, sigma shift 12/3), roughly
#2.5x faster than the 20-step render. For maximum fidelity use "Reference To Video (MiniMax H3
#Quality) 5s" (the full 20-step graph).
#Unlike image-to-video, the output does not start on an input frame; the references lock identity/style.
#Prompts must refer to the photos as <Picture 1> (and <Picture 2>..<Picture 9> when supplied).
#Extra photos are OPTIONAL and pruned from the graph when absent; AI Chat wires them via
#chat_image2..chat_image9 or attachment2..attachment9. Give each photo ONE job (identity,
#second character, setting/style). Multiple photos of the SAME person are fine (better identity
#lock) - the prompt should treat them as one character ("the man from <Picture 1> and <Picture 2>").
#Default canvas 864x480 (~0.4MP). For identity-critical faces the AI Chat action may pass
#width/height (e.g. 1152x640 landscape) - the dimension override appends here automatically.
#H3's trained max is ~1.03MP (768 short edge, cap 1344x768); cost scales with pixel count.
%vid_width%="864"
%vid_height%="480"
%vid_length%="124"

#OPTIONAL standalone AUDIO references (wired by AI Chat audio=/audio2=/audio3=, CLI --audio):
#audio1..audio3 -> inputs 12-14 -> <Audio N> prompt tags, numbered AFTER any clip soundtracks
#(e.g. with one clip staged: <Audio 1> = the clip's soundtrack, <Audio 2> = first standalone file).
#Use for per-character voice samples or a music bed; at most 3 audio refs TOTAL per render
#(clip soundtracks + standalone), the model's ref_video_audios ceiling.
ref_multi_to_video_minimax_h3_turbo.json @upload|image1|input3| @upload|image2|input4|optional| @upload|image3|input5|optional| @upload|image4|input6|optional| @upload|image5|input7|optional| @upload|image6|input8|optional| @upload|image7|input9|optional| @upload|image8|input10|optional| @upload|image9|input11|optional| @upload|audio1|input12|optional| @upload|audio2|input13|optional| @upload|audio3|input14|optional| @replace|"width": 864|"width": %vid_width%| @replace|"height": 480|"height": %vid_height%| @replace|"length": 124|"length": %vid_length%|
COMMAND_END
