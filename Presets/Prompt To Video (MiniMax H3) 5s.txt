COMMAND_START|joblist
#Generate ~5-second video with native stereo audio from text using MiniMax H3.
#Turbo distill LoRA workflow (8 steps, no Spectrum cache). Since 2026-08-17 AI Chat's
#direct-t2v default is "Prompt To Video (MiniMax H3 Turbo Cache) 5s"; this is the
#no-cache fallback. Use "Prompt To Video (MiniMax H3 Quality) 5s" for the 20-step render.
%width%="864"
%height%="480"
%length%="124"

text_to_video_minimax_h3_turbo.json @replace|"width": 864|"width": %width%| @replace|"height": 480|"height": %height%| @replace|"length": 124|"length": %length%|
COMMAND_END
