COMMAND_START|joblist
#Generate a NEW ~5-second video with audio using a loaded VIDEO as the reference (MiniMax H3 Ref2VA).
#QUALITY: the full 20-step render (~2x the turbo default "Reference Video To Video (MiniMax H3) 5s").
#Use for explicit high-quality / maximum-fidelity requests.
#The output carries the subject/motion/style of the source clip; it does not restyle or continue it.
#The clip's SOUNDTRACK is referenced too (up to 15s of frames+audio at 24fps), so music/voices can
#carry over. Prompts refer to the clip as <Video 1> and its soundtrack as <Audio 1>.
#OPTIONAL extra references, pruned from the graph when absent (wired by AI Chat):
#  video2 (second Movie bubble via chat_image2) -> <Video 2> / <Audio 2>
#  image2..image10 (photo stills via chat_image2..10 or attachment2..10) -> <Picture 1>..<Picture 9>
#Give each reference ONE job (e.g. <Video 1> = motion/camera/voice, <Picture 1> = person identity).
#Multiple photos of the SAME person are fine (better identity lock) - the prompt should still
#give them one combined job ("the man from <Picture 1> and <Picture 2>" = one character).
#Silent clips are handled automatically: AI Chat prunes that clip's audio wire (@prune_input) and
#H3 synthesizes the soundtrack from the prompt alone; a manual run on a silent clip still fails in
#the video loader ("VHS failed to extract audio from ...").
#NOTE: no @replace|"length"| here on purpose - AI Chat's video_to_video path overrides any exposed
#length to match the SOURCE duration (at WAN's 16fps cadence), which is wrong for a reference
#generation. Duration stays the workflow's 124 frames (~5s @24fps).
#Default canvas 864x480 (~0.4MP). For identity-critical faces the AI Chat action may pass
#width/height (e.g. 1152x640 landscape) - the dimension override appends here automatically.
#H3's trained max is ~1.03MP (768 short edge, cap 1344x768); cost scales with pixel count.
%vid_width%="864"
%vid_height%="480"

#OPTIONAL standalone AUDIO references (wired by AI Chat audio=/audio2=/audio3=, CLI --audio):
#audio1..audio3 -> inputs 12-14 -> <Audio N> prompt tags, numbered AFTER any clip soundtracks
#(e.g. with one clip staged: <Audio 1> = the clip's soundtrack, <Audio 2> = first standalone file).
#STYLE references (voice character/music/ambience nudged toward the sample, not clones).
#Up to 3 standalone files (the node's separate ref_audios group), plus each staged clip's
#own soundtrack (index-paired ref_video_audios group).
ref_multi_to_video_minimax_h3.json @upload|video|input1| @upload|video2|input2|optional| @upload|image2|input3|optional| @upload|image3|input4|optional| @upload|image4|input5|optional| @upload|image5|input6|optional| @upload|image6|input7|optional| @upload|image7|input8|optional| @upload|image8|input9|optional| @upload|image9|input10|optional| @upload|image10|input11|optional| @upload|audio1|input12|optional| @upload|audio2|input13|optional| @upload|audio3|input14|optional| @replace|"width": 864|"width": %vid_width%| @replace|"height": 480|"height": %vid_height%|
COMMAND_END
